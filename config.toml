# Sleeper league to build. Overridden by SLEEPER_LEAGUE_ID env var, or --league/--season CLI flags.
league_id = "1378825622272356352"  # 2026 season
# Used only for the link in `ffpr blurb` output.
site_url = "https://sconnors00.github.io/sleeper-power-rankings/"

[weights]
win_pct = 0.30
allplay_pct = 0.25
pf_norm = 0.30
form_norm = 0.15

[form]
window = 3
